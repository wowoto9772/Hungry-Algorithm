#include <stdio.h>
#include <string.h>
#include <algorithm>
#include <set>

using namespace std;

char a[1503], b[1503];
int da[28], db[28];

int main() {

	scanf("%s %s", a, b);

	int a_ = strlen(a), b_ = strlen(b);

	int m = min(a_, b_);

}