#include <stdio.h>
#include <queue>

using namespace std;

bool used[33];
int in[33];

int main(){
	int n;
	scanf("%d", &n);


	int m;
	scanf("%d", &m);

	for (int i = 1; i <= m; i++){
		int a, b;
		in[b]++;
		used[a] = used[b] = true;
	}

	for (int i = 1; i <= n; i++){

	}
}

// 조합론적으로 들어가야할듯 어렵다